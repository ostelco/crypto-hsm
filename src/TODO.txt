1.  Run through the processes of generating CSRs and making them flow through the system
    (keeping the artefacts in a compeltely separate code hierarchy).
2.  Move the whole thing to the "HSM"
3.  Think about how to use a pkcs #11 compliant HSM instead of local storage, and if possible
    implement things that way.
4.  Think about porting the whole thing to golang and rapsberry pi, or something equally insular.
5.  Think about running the pkcs#11 HSM in a public cloud of some soet.

